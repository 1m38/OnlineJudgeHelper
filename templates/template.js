function Main(input) {
    // 1行目がinput[0], 2行目がinput[1], ...に入る
    input = input.split("\n");
    tmp = input[1].split(" ");

    console.log();
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
